apiVersion: v1
kind: Service
metadata:
  name: {{ include "mc-multiserver.fullname" . }}
  labels:
    {{- include "mc-multiserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mc-multiserver.selectorLabels" . | nindent 4 }}
